# Fan Yang — Academic Homepage

A lightweight, responsive academic homepage designed for GitHub Pages.

## Publish with GitHub Pages

Create a public repository named `<your-github-username>.github.io`. Upload the contents of the `dist` folder to the repository root, then open `https://<your-github-username>.github.io/`.

No build step or external dependency is required.
